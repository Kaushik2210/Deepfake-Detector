# VeriFrame evaluation — 2026-09-07

Generated by `services/inference/eval/run.py`. Every number in the product must trace to a row in this file; nothing here is hand-entered.

## Read this before quoting any number

- **Sample size.** 2000 calibration images, 1000 weight-validation images, and 1000 final held-out reporting images. That is enough for a stable AUC and for fitting a single calibration parameter. It is *not* enough to measure true-positive rate at very low false-positive rates; those rows say so explicitly rather than showing a number built from a handful of events.
- **Possible contamination.** The classifier's training data is not published in full, so we cannot rule out overlap between it and these evaluation corpora. If overlap exists, these figures are optimistic.
- **These are not accuracy claims for arbitrary media.** They describe performance on two specific corpora. Deepfake detectors degrade sharply on generators they have not seen, and the generators in circulation change faster than any fixed evaluation set.
- **Fusion weights come from a genuine cross-dataset split, not the calibration corpus.** Earlier runs derived weights from calibration-split AUC — in-distribution performance — which does not predict cross-dataset generalisation: it once gave the frequency stream 86% of the fusion weight, and that stream then generalised worse than spatial did on the reporting split (see DECISIONS.md). The reporting corpus is now split once, stratified by label, into a weight-validation half and a final reporting half. Weights are derived from the validation half; every headline number below comes from the final half, which played no part in choosing them.

## Protocol

Calibration was fitted on **deepfakeface** and every headline figure is reported on **df40faces**, which shares no images with it. The df40faces pool is itself split once, stratified by label, into a weight-validation half (used only to derive the fusion weights below) and a final held-out half (used only for the headline numbers). In-dataset figures appear further down purely for contrast — they are the numbers that flatter, and the ones most published results quote.

| Corpus | Role | Licence | Commercial use | Scored | No face detected |
|---|---|---|---|---|---|
| `OpenRL/DeepFakeFace` | calibration | Apache-2.0 | yes | 2000 | 22 |
| `pujanpaudel/deepfake_face_classification` | weight validation | CC BY-NC 4.0 | **no** | 1000 | — |
| `pujanpaudel/deepfake_face_classification` | final reporting | CC BY-NC 4.0 | **no** | 1000 | — |

## Cross-dataset results (headline)

### Stream: spatial

n = 1000 (500 manipulated, 500 authentic)

| Metric | Value | Note |
|---|---|---|
| AUC | 0.6641 | 95% CI 0.6324–0.6979 (bootstrap) |
| EER | 0.3850 | at threshold 0.4425 |
| TPR @ FPR=1.0% | 0.0160 | based on ~5 false positives out of 500 authentic samples; fewer than 10 events, so this estimate is unstable |
| TPR @ FPR=0.1% | not measurable | not measurable: 500 authentic samples resolve no finer than 0.20% FPR, so a 0.10% figure would rest on fewer than one false positive |
| ECE before calibration | 0.1434 | gap between claimed and observed |
| ECE after calibration | 0.0812 | temperature scaling applied |
| Mean score, manipulated | 0.5117 | |
| Mean score, authentic | 0.3666 | |

Calibration reduced ECE from 0.1434 to 0.0812. AUC is unchanged by construction — temperature scaling alters confidence, not ranking.

### Stream: frequency

n = 1000 (500 manipulated, 500 authentic)

| Metric | Value | Note |
|---|---|---|
| AUC | 0.5752 | 95% CI 0.5433–0.6117 (bootstrap) |
| EER | 0.4420 | at threshold 0.3844 |
| TPR @ FPR=1.0% | 0.0640 | based on ~5 false positives out of 500 authentic samples; fewer than 10 events, so this estimate is unstable |
| TPR @ FPR=0.1% | not measurable | not measurable: 500 authentic samples resolve no finer than 0.20% FPR, so a 0.10% figure would rest on fewer than one false positive |
| ECE before calibration | 0.1070 | gap between claimed and observed |
| ECE after calibration | 0.1435 | temperature scaling applied |
| Mean score, manipulated | 0.4073 | |
| Mean score, authentic | 0.3787 | |

Calibration did **not** improve ECE on this corpus (0.1070 → 0.1435), which is what you would expect when the calibration corpus and the reporting corpus differ in difficulty. This is the honest outcome of a cross-dataset protocol.

## Is the gap between streams real?

A paired bootstrap significance test on the final held-out split: spatial scores 0.6641 AUC, frequency scores 0.5752, a difference of 0.0889 with a 95% CI of [0.0397, 0.1365] (p=0.0000).

spatial beats frequency by 0.0889 AUC; significant at p=0.0000 (the 95% CI on the difference excludes zero)

## Weight-selection validation

The AUC each stream measures on the weight-validation half above -- a held-out slice of **df40faces**, disjoint from the final reporting half -- and the only number the fusion weights below are derived from.

| Stream | Validation AUC (this decides the weight) |
|---|---|
| spatial | 0.6631 |
| frequency | 0.5983 |

## Fusion weights

Derived from measured AUC on the weight-validation half of the reporting corpus -- a genuine cross-dataset measurement, not the calibration corpus's in-distribution AUC. Weight is proportional to how far a stream sits above chance, normalised across streams. A stream at or below chance receives zero.

| Stream | Validation AUC | Weight | Rationale |
|---|---|---|---|
| spatial | 0.6631 | 0.6238 | AUC 0.663, i.e. 0.163 above chance, normalised across streams |
| frequency | 0.5983 | 0.3762 | AUC 0.598, i.e. 0.098 above chance, normalised across streams |

### Is that weight split stable, or a lucky draw?

A bootstrap over the weight-validation split itself (500 resamples, weights rederived from the same procedure each time): the median weight and the middle 80% of resamples (p10-p90) for each stream. A wide or overlapping range means the exact split above should not be read as a settled number — a different, equally valid draw of validation clips could have landed noticeably differently.

| Stream | Median weight | p10-p90 |
|---|---|---|
| spatial | 0.6229 | 0.5562-0.6864 |
| frequency | 0.3771 | 0.3136-0.4438 |

### Stream D (provenance) — not measurable on these corpora

Stream D reads C2PA manifests and generator metadata. Neither corpus ships images carrying either, so no AUC can be computed for it here. That is a property of the evaluation data, not evidence the stream does not work — but it does mean its fusion weight cannot be derived from measurement, so it is excluded from weighted fusion and used only through its override path.

It fired on 0 calibration and 0 reporting images.

## Calibration parameters

| Stream | Temperature |
|---|---|
| spatial | 10.0000 |
| frequency | 0.7200 |

Temperature > 1 softens an overconfident model; < 1 sharpens an underconfident one. Fitted by minimising negative log-likelihood on the calibration split only.

## Robustness to recompression

AUC on the reporting corpus after re-encoding at each JPEG quality. Most media arrives recompressed, so this is closer to deployment conditions than the headline figure.

| JPEG quality | frequency | spatial |
|---|---|---|
| q90 | 0.5960 | 0.6042 |
| q60 | 0.4451 | 0.5907 |
| q30 | 0.3769 | 0.5947 |

## In-dataset results (for contrast only)

Measured on **deepfakeface**, the same corpus calibration was fitted on. These are not a performance claim. They are included so the gap against the cross-dataset figures above is visible, since that gap is the whole reason the cross-dataset protocol is mandatory.

| Stream | AUC | EER |
|---|---|---|
| spatial | 0.5369 | 0.4788 |
| frequency | 0.7208 | 0.3420 |

---

Detection is advisory. These figures describe measured behaviour on fixed corpora and are not proof about any individual image.
