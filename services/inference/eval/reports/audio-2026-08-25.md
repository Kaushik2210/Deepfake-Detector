# VeriFrame audio evaluation — 2026-08-25

Generated by `services/inference/eval/audio_run.py`. Every number in the product must trace to a row in this file; nothing here is hand-entered.

## Read this before quoting any number

- **Sample size.** 60 clips were drawn per corpus. That is enough for a stable AUC and for fitting a single calibration parameter. It is *not* enough to measure true-positive rate at very low false-positive rates; those rows say so explicitly rather than showing a number built from a handful of events.
- **The calibration corpus is what AASIST was trained on.** Unlike the image classifier, AASIST's training data provenance is fully documented (ASVspoof2019 LA), so 'in-dataset' here is a known quantity, not a contamination risk of unknown extent.
- **These are not accuracy claims for arbitrary audio.** They describe performance on two specific corpora of studio-adjacent TTS/voice-conversion attacks. Real-world audio — phone calls, compressed voice notes, background noise — is exactly the out-of-distribution condition `audio_envelope.py` flags with a confidence penalty, and generators in circulation change faster than any fixed evaluation set.

## Protocol

Calibration was fitted on **asvspoof2019** and every headline figure is reported on **asvspoof2021**, which shares no utterances with it and uses different attack algorithms and codec/transmission conditions. In-dataset figures appear further down purely for contrast — they are the numbers that flatter, and the ones most published results quote.

| Corpus | Role | Licence | Commercial use | Scored |
|---|---|---|---|---|
| `Bisher/ASVspoof_2019_LA` | calibration | ODC-By | yes | 60 |
| `Bisher/ASVspoof_2021_DF` | reporting | ODC-By | yes | 31 |

## Cross-dataset results (headline)

### Stream: audio

n = 31 (17 spoof, 14 bonafide)

| Metric | Value | Note |
|---|---|---|
| AUC | 0.9622 | 95% CI 0.8795–1.0000 (bootstrap) |
| EER | 0.0651 | at threshold 0.9671 |
| TPR @ FPR=1.0% | not measurable | not measurable: 14 authentic samples resolve no finer than 7.14% FPR, so a 1.00% figure would rest on fewer than one false positive |
| TPR @ FPR=0.1% | not measurable | not measurable: 14 authentic samples resolve no finer than 7.14% FPR, so a 0.10% figure would rest on fewer than one false positive |
| ECE before calibration | 0.0829 | gap between claimed and observed |
| ECE after calibration | 0.0879 | temperature scaling applied |
| Mean score, spoof | 0.9389 | |
| Mean score, bonafide | 0.1270 | |

Calibration did **not** improve ECE on this corpus (0.0829 → 0.0879), which is what you would expect when the calibration corpus and the reporting corpus differ in difficulty. This is the honest outcome of a cross-dataset protocol.

## Fusion weight

Derived from measured validation AUC, never hand-picked. There is currently one audio stream, so this is normally 1.0 by construction unless the measured AUC is at or below chance.

| Stream | Validation AUC | Weight | Rationale |
|---|---|---|---|
| audio | 1.0000 | 1.0000 | AUC 1.000, i.e. 0.500 above chance, normalised across streams |

## Calibration parameters

| Stream | Temperature |
|---|---|
| audio | 0.8100 |

Temperature > 1 softens an overconfident model; < 1 sharpens an underconfident one. Fitted by minimising negative log-likelihood on the calibration split only.

## In-dataset results (for contrast only)

Measured on **asvspoof2019**, the same corpus calibration was fitted on and AASIST itself was trained on. These are not a performance claim. They are included so the gap against the cross-dataset figures above is visible, since that gap is the whole reason the cross-dataset protocol is mandatory.

| Stream | AUC | EER |
|---|---|---|
| audio | 1.0000 | 0.0000 |

---

Detection is advisory. These figures describe measured behaviour on fixed corpora and are not proof about any individual clip.
