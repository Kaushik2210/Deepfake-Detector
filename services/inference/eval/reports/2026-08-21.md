# VeriFrame evaluation — 2026-08-21

Generated by `services/inference/eval/run.py`. Every number in the product must trace to a row in this file; nothing here is hand-entered.

## Read this before quoting any number

- **Sample size.** 1200 images were drawn per corpus. That is enough for a stable AUC and for fitting a single calibration parameter. It is *not* enough to measure true-positive rate at very low false-positive rates; those rows say so explicitly rather than showing a number built from a handful of events.
- **Possible contamination.** The classifier's training data is not published in full, so we cannot rule out overlap between it and these evaluation corpora. If overlap exists, these figures are optimistic.
- **These are not accuracy claims for arbitrary media.** They describe performance on two specific corpora. Deepfake detectors degrade sharply on generators they have not seen, and the generators in circulation change faster than any fixed evaluation set.

## Protocol

Calibration was fitted on **deepfakeface** and every headline figure is reported on **df40faces**, which shares no images with it. In-dataset figures appear further down purely for contrast — they are the numbers that flatter, and the ones most published results quote.

| Corpus | Role | Licence | Commercial use | Scored | No face detected |
|---|---|---|---|---|---|
| `OpenRL/DeepFakeFace` | calibration | Apache-2.0 | yes | 1200 | 15 |
| `pujanpaudel/deepfake_face_classification` | reporting | CC BY-NC 4.0 | **no** | 1200 | 0 |

## Cross-dataset results (headline)

### Stream: spatial

n = 1200 (600 manipulated, 600 authentic)

| Metric | Value | Note |
|---|---|---|
| AUC | 0.6633 | 95% CI 0.6333–0.6952 (bootstrap) |
| EER | 0.3833 | at threshold 0.4254 |
| TPR @ FPR=1.0% | 0.0200 | based on ~6 false positives out of 600 authentic samples; fewer than 10 events, so this estimate is unstable |
| TPR @ FPR=0.1% | not measurable | not measurable: 600 authentic samples resolve no finer than 0.17% FPR, so a 0.10% figure would rest on fewer than one false positive |
| ECE before calibration | 0.1435 | gap between claimed and observed |
| ECE after calibration | 0.0749 | temperature scaling applied |
| Mean score, manipulated | 0.5043 | |
| Mean score, authentic | 0.3582 | |

Calibration reduced ECE from 0.1435 to 0.0749. AUC is unchanged by construction — temperature scaling alters confidence, not ranking.

### Stream: frequency

n = 1200 (600 manipulated, 600 authentic)

| Metric | Value | Note |
|---|---|---|
| AUC | 0.5890 | 95% CI 0.5537–0.6207 (bootstrap) |
| EER | 0.4008 | at threshold 0.3891 |
| TPR @ FPR=1.0% | 0.0533 | based on ~6 false positives out of 600 authentic samples; fewer than 10 events, so this estimate is unstable |
| TPR @ FPR=0.1% | not measurable | not measurable: 600 authentic samples resolve no finer than 0.17% FPR, so a 0.10% figure would rest on fewer than one false positive |
| ECE before calibration | 0.1233 | gap between claimed and observed |
| ECE after calibration | 0.1358 | temperature scaling applied |
| Mean score, manipulated | 0.4079 | |
| Mean score, authentic | 0.3828 | |

Calibration did **not** improve ECE on this corpus (0.1233 → 0.1358), which is what you would expect when the calibration corpus and the reporting corpus differ in difficulty. This is the honest outcome of a cross-dataset protocol.

## Fusion weights

Derived from measured validation AUC, never hand-picked. Weight is proportional to how far a stream sits above chance, normalised across streams. A stream at or below chance receives zero.

| Stream | Validation AUC | Weight | Rationale |
|---|---|---|---|
| frequency | 0.7132 | 0.8642 | AUC 0.713, i.e. 0.213 above chance, normalised across streams |
| spatial | 0.5335 | 0.1358 | AUC 0.534, i.e. 0.034 above chance, normalised across streams |

### Stream D (provenance) — not measurable on these corpora

Stream D reads C2PA manifests and generator metadata. Neither corpus ships images carrying either, so no AUC can be computed for it here. That is a property of the evaluation data, not evidence the stream does not work — but it does mean its fusion weight cannot be derived from measurement, so it is excluded from weighted fusion and used only through its override path.

It fired on 0 calibration and 0 reporting images.

## Calibration parameters

| Stream | Temperature |
|---|---|
| spatial | 10.0000 |
| frequency | 0.7500 |

Temperature > 1 softens an overconfident model; < 1 sharpens an underconfident one. Fitted by minimising negative log-likelihood on the calibration split only.

## Robustness to recompression

AUC on the reporting corpus after re-encoding at each JPEG quality. Most media arrives recompressed, so this is closer to deployment conditions than the headline figure.

| JPEG quality | frequency | spatial |
|---|---|---|
| q90 | 0.5923 | 0.6449 |
| q70 | 0.5013 | 0.6419 |
| q50 | 0.4541 | 0.6378 |
| q30 | 0.4078 | 0.6192 |

## In-dataset results (for contrast only)

Measured on **deepfakeface**, the same corpus calibration was fitted on. These are not a performance claim. They are included so the gap against the cross-dataset figures above is visible, since that gap is the whole reason the cross-dataset protocol is mandatory.

| Stream | AUC | EER |
|---|---|---|
| spatial | 0.5335 | 0.4819 |
| frequency | 0.7132 | 0.3417 |

---

Detection is advisory. These figures describe measured behaviour on fixed corpora and are not proof about any individual image.
