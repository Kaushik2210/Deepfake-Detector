# VeriFrame audio evaluation — 2026-08-28

Generated by `services/inference/eval/audio_run.py`. Every number in the product must trace to a row in this file; nothing here is hand-entered.

## Read this before quoting any number

- **Sample size.** 100 calibration clips, 50 weight-validation clips, and 50 final held-out reporting clips. That is enough for a stable AUC and for fitting a single calibration parameter. It is *not* enough to measure true-positive rate at very low false-positive rates; those rows say so explicitly rather than showing a number built from a handful of events.
- **The calibration corpus is what AASIST was trained on.** Unlike the image classifier, AASIST's training data provenance is fully documented (ASVspoof2019 LA), so 'in-dataset' here is a known quantity, not a contamination risk of unknown extent.
- **Fusion weights come from a genuine cross-dataset split, not the calibration corpus.** Earlier runs derived weights from calibration-split AUC — in-distribution performance — which does not predict cross-dataset generalisation and previously produced a fusion weight that made cross-dataset AUC *worse* (see DECISIONS.md). The reporting corpus is now split once, stratified by label, into a weight-validation half and a final reporting half. Weights are derived from the validation half; every headline number below comes from the final half, which played no part in choosing them.
- **These are not accuracy claims for arbitrary audio.** They describe performance on two specific corpora of studio-adjacent TTS/voice-conversion attacks. Real-world audio — phone calls, compressed voice notes, background noise — is exactly the out-of-distribution condition `audio_envelope.py` flags with a confidence penalty, and generators in circulation change faster than any fixed evaluation set.

## Protocol

Calibration was fitted on **asvspoof2019** and every headline figure is reported on **asvspoof2021**, which shares no utterances with it and uses different attack algorithms and codec/transmission conditions. The asvspoof2021 pool is itself split once, stratified by label, into a weight-validation half (used only to derive the fusion weight below) and a final held-out half (used only for the headline numbers). In-dataset figures appear further down purely for contrast — they are the numbers that flatter, and the ones most published results quote.

| Corpus | Role | Licence | Commercial use | Scored |
|---|---|---|---|---|
| `Bisher/ASVspoof_2019_LA` | calibration | ODC-By | yes | 100 |
| `Bisher/ASVspoof_2021_DF` | weight validation | ODC-By | yes | 50 |
| `Bisher/ASVspoof_2021_DF` | final reporting | ODC-By | yes | 50 |

## Cross-dataset results (headline)

### Stream: audio (AASIST)

n = 50 (25 spoof, 25 bonafide)

| Metric | Value | Note |
|---|---|---|
| AUC | 0.9856 | 95% CI 0.9500–1.0000 (bootstrap) |
| EER | 0.0200 | at threshold 0.0106 |
| TPR @ FPR=1.0% | not measurable | not measurable: 25 authentic samples resolve no finer than 4.00% FPR, so a 1.00% figure would rest on fewer than one false positive |
| TPR @ FPR=0.1% | not measurable | not measurable: 25 authentic samples resolve no finer than 4.00% FPR, so a 0.10% figure would rest on fewer than one false positive |
| ECE before calibration | 0.0957 | gap between claimed and observed |
| ECE after calibration | 0.0901 | temperature scaling applied |
| Mean score, spoof | 0.8079 | |
| Mean score, bonafide | 0.0006 | |

Calibration reduced ECE from 0.0957 to 0.0901. AUC is unchanged by construction — temperature scaling alters confidence, not ranking.

### Stream: audio_frequency (harmonics-to-noise ratio (this project's own, unsupervised))

n = 50 (25 spoof, 25 bonafide)

| Metric | Value | Note |
|---|---|---|
| AUC | 0.7072 | 95% CI 0.5471–0.8451 (bootstrap) |
| EER | 0.3800 | at threshold 0.3719 |
| TPR @ FPR=1.0% | not measurable | not measurable: 25 authentic samples resolve no finer than 4.00% FPR, so a 1.00% figure would rest on fewer than one false positive |
| TPR @ FPR=0.1% | not measurable | not measurable: 25 authentic samples resolve no finer than 4.00% FPR, so a 0.10% figure would rest on fewer than one false positive |
| ECE before calibration | 0.1356 | gap between claimed and observed |
| ECE after calibration | 0.1195 | temperature scaling applied |
| Mean score, spoof | 0.4449 | |
| Mean score, bonafide | 0.3271 | |

Calibration reduced ECE from 0.1356 to 0.1195. AUC is unchanged by construction — temperature scaling alters confidence, not ranking.

## Does the second stream actually help?

The whole reason `audio_frequency` (harmonics-to-noise ratio, this project's own unsupervised measurement -- see DECISIONS.md) was added: does fusing it with AASIST improve on AASIST alone, on the held-out corpus? Reported honestly either way.

| | AASIST alone | Fused (AASIST + audio_frequency) | Δ |
|---|---|---|---|
| AUC | 0.9856 | 0.9792 | -0.0064 |
| EER | 0.0200 | 0.0800 | +0.0600 (lower is better) |

Fusion **did not improve** cross-dataset AUC (Δ -0.0064). AASIST alone generalises better on this corpus than the fused score does. This is still a legitimate, reportable outcome of the experiment, not a failure to hide -- see DECISIONS.md.

## Weight-selection validation

The AUC each stream measures on the weight-validation half above -- a held-out slice of **asvspoof2021**, disjoint from the final reporting half -- and the only number the fusion weight below is derived from.

| Stream | Validation AUC (this decides the weight) |
|---|---|
| audio | 0.9760 |
| audio_frequency | 0.6528 |

## Fusion weight

Derived from measured AUC on the weight-validation half of the reporting corpus -- a genuine cross-dataset measurement, not the calibration corpus's in-distribution AUC (see 'Read this before quoting any number' above for why that distinction matters). There is currently one audio stream with signal, so this is normally close to 1.0 by construction unless the measured AUC is at or below chance.

| Stream | Validation AUC | Weight | Rationale |
|---|---|---|---|
| audio | 0.9760 | 0.7570 | AUC 0.976, i.e. 0.476 above chance, normalised across streams |
| audio_frequency | 0.6528 | 0.2430 | AUC 0.653, i.e. 0.153 above chance, normalised across streams |

## Calibration parameters

| Stream | Temperature |
|---|---|
| audio | 1.8000 |
| audio_frequency | 0.9700 |

Temperature > 1 softens an overconfident model; < 1 sharpens an underconfident one. Fitted by minimising negative log-likelihood on the calibration split only.

## Robustness to background noise

AUC on the reporting corpus after adding white Gaussian noise at each signal-to-noise ratio. Real-world audio — phone calls, voice notes, ambient recordings — routinely arrives with far more noise than a studio recording, so this is closer to deployment conditions than the headline figure.

| SNR (dB) | audio | audio_frequency |
|---|---|---|
| 25.0 | 0.9764 | 0.8014 |
| 15.0 | 0.9760 | 0.7966 |

## In-dataset results (for contrast only)

Measured on **asvspoof2019**, the same corpus calibration was fitted on and AASIST itself was trained on. These are not a performance claim. They are included so the gap against the cross-dataset figures above is visible, since that gap is the whole reason the cross-dataset protocol is mandatory.

| Stream | AUC | EER |
|---|---|---|
| audio | 1.0000 | 0.0000 |
| audio_frequency | 0.7576 | 0.2500 |

---

Detection is advisory. These figures describe measured behaviour on fixed corpora and are not proof about any individual clip.
