# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# env
.env
.env.local
.env.*.local

# media / model artifacts (never commit real media or weights)
*.onnx
*.pt
*.pth
uploads/
tmp/
.model_cache/
.artifacts/

# python tooling
.pytest_cache/
.ruff_cache/

# datasets (license-gated, never commit)
datasets/
eval/data/

# resumable per-image score cache from the eval harness
.score_cache/

# editor / OS
.vscode/*
!.vscode/extensions.json
# Claude Code session/tooling config -- local to each developer's machine.
.claude/
.idea/
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# eval artifacts (datasets and reports contain no media, but keep caches out)
services/inference/eval/.cache/
